'use strict';

var midnightJsTypes = require('@midnight-ntwrk/midnight-js-types');
var ledger = require('@midnight-ntwrk/ledger');
var compactRuntime = require('@midnight-ntwrk/compact-runtime');
var index_js = require('@apollo/client/core/index.js');
var index_js$1 = require('@apollo/client/link/core/index.js');
var index_js$4 = require('@apollo/client/link/http/index.js');
var index_js$2 = require('@apollo/client/utilities/index.js');
var index_js$3 = require('@apollo/client/link/subscriptions/index.js');
var buffer = require('buffer');
var Rx = require('rxjs');
var fetch = require('cross-fetch');
var graphqlWs = require('graphql-ws');
var ws = require('isomorphic-ws');
var midnightJsUtils = require('@midnight-ntwrk/midnight-js-utils');
var midnightJsNetworkId = require('@midnight-ntwrk/midnight-js-network-id');

function _interopNamespaceDefault(e) {
    var n = Object.create(null);
    if (e) {
        Object.keys(e).forEach(function (k) {
            if (k !== 'default') {
                var d = Object.getOwnPropertyDescriptor(e, k);
                Object.defineProperty(n, k, d.get ? d : {
                    enumerable: true,
                    get: function () { return e[k]; }
                });
            }
        });
    }
    n.default = e;
    return Object.freeze(n);
}

var Rx__namespace = /*#__PURE__*/_interopNamespaceDefault(Rx);
var ws__namespace = /*#__PURE__*/_interopNamespaceDefault(ws);

const BlockHashQueryDocument = { "kind": "Document", "definitions": [{ "kind": "OperationDefinition", "operation": "query", "name": { "kind": "Name", "value": "BLOCK_HASH_QUERY" }, "variableDefinitions": [{ "kind": "VariableDefinition", "variable": { "kind": "Variable", "name": { "kind": "Name", "value": "offset" } }, "type": { "kind": "NamedType", "name": { "kind": "Name", "value": "BlockOffsetInput" } } }], "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "Field", "name": { "kind": "Name", "value": "block" }, "arguments": [{ "kind": "Argument", "name": { "kind": "Name", "value": "offset" }, "value": { "kind": "Variable", "name": { "kind": "Name", "value": "offset" } } }], "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "Field", "name": { "kind": "Name", "value": "height" } }, { "kind": "Field", "name": { "kind": "Name", "value": "hash" } }] } }] } }] };
const TxIdQueryDocument = { "kind": "Document", "definitions": [{ "kind": "OperationDefinition", "operation": "query", "name": { "kind": "Name", "value": "TX_ID_QUERY" }, "variableDefinitions": [{ "kind": "VariableDefinition", "variable": { "kind": "Variable", "name": { "kind": "Name", "value": "identifier" } }, "type": { "kind": "NonNullType", "type": { "kind": "NamedType", "name": { "kind": "Name", "value": "String" } } } }], "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "Field", "name": { "kind": "Name", "value": "transactions" }, "arguments": [{ "kind": "Argument", "name": { "kind": "Name", "value": "identifier" }, "value": { "kind": "Variable", "name": { "kind": "Name", "value": "identifier" } } }], "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "Field", "name": { "kind": "Name", "value": "raw" } }, { "kind": "Field", "name": { "kind": "Name", "value": "applyStage" } }, { "kind": "Field", "name": { "kind": "Name", "value": "hash" } }, { "kind": "Field", "name": { "kind": "Name", "value": "block" }, "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "Field", "name": { "kind": "Name", "value": "height" } }, { "kind": "Field", "name": { "kind": "Name", "value": "hash" } }] } }] } }] } }] };
const DeployTxQueryDocument = { "kind": "Document", "definitions": [{ "kind": "OperationDefinition", "operation": "query", "name": { "kind": "Name", "value": "DEPLOY_TX_QUERY" }, "variableDefinitions": [{ "kind": "VariableDefinition", "variable": { "kind": "Variable", "name": { "kind": "Name", "value": "address" } }, "type": { "kind": "NonNullType", "type": { "kind": "NamedType", "name": { "kind": "Name", "value": "String" } } } }], "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "Field", "name": { "kind": "Name", "value": "contract" }, "arguments": [{ "kind": "Argument", "name": { "kind": "Name", "value": "address" }, "value": { "kind": "Variable", "name": { "kind": "Name", "value": "address" } } }], "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "InlineFragment", "typeCondition": { "kind": "NamedType", "name": { "kind": "Name", "value": "ContractDeploy" } }, "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "Field", "name": { "kind": "Name", "value": "transaction" }, "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "Field", "name": { "kind": "Name", "value": "raw" } }, { "kind": "Field", "name": { "kind": "Name", "value": "applyStage" } }, { "kind": "Field", "name": { "kind": "Name", "value": "hash" } }, { "kind": "Field", "name": { "kind": "Name", "value": "identifiers" } }, { "kind": "Field", "name": { "kind": "Name", "value": "contractCalls" }, "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "Field", "name": { "kind": "Name", "value": "address" } }] } }, { "kind": "Field", "name": { "kind": "Name", "value": "block" }, "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "Field", "name": { "kind": "Name", "value": "height" } }, { "kind": "Field", "name": { "kind": "Name", "value": "hash" } }] } }] } }] } }, { "kind": "InlineFragment", "typeCondition": { "kind": "NamedType", "name": { "kind": "Name", "value": "ContractCall" } }, "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "Field", "name": { "kind": "Name", "value": "deploy" }, "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "Field", "name": { "kind": "Name", "value": "transaction" }, "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "Field", "name": { "kind": "Name", "value": "raw" } }, { "kind": "Field", "name": { "kind": "Name", "value": "applyStage" } }, { "kind": "Field", "name": { "kind": "Name", "value": "hash" } }, { "kind": "Field", "name": { "kind": "Name", "value": "identifiers" } }, { "kind": "Field", "name": { "kind": "Name", "value": "contractCalls" }, "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "Field", "name": { "kind": "Name", "value": "address" } }] } }, { "kind": "Field", "name": { "kind": "Name", "value": "block" }, "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "Field", "name": { "kind": "Name", "value": "height" } }, { "kind": "Field", "name": { "kind": "Name", "value": "hash" } }] } }] } }] } }] } }] } }] } }] };
const DeployContractStateTxQueryDocument = { "kind": "Document", "definitions": [{ "kind": "OperationDefinition", "operation": "query", "name": { "kind": "Name", "value": "DEPLOY_CONTRACT_STATE_TX_QUERY" }, "variableDefinitions": [{ "kind": "VariableDefinition", "variable": { "kind": "Variable", "name": { "kind": "Name", "value": "address" } }, "type": { "kind": "NonNullType", "type": { "kind": "NamedType", "name": { "kind": "Name", "value": "String" } } } }], "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "Field", "name": { "kind": "Name", "value": "contract" }, "arguments": [{ "kind": "Argument", "name": { "kind": "Name", "value": "address" }, "value": { "kind": "Variable", "name": { "kind": "Name", "value": "address" } } }], "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "InlineFragment", "typeCondition": { "kind": "NamedType", "name": { "kind": "Name", "value": "ContractDeploy" } }, "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "Field", "name": { "kind": "Name", "value": "state" } }] } }, { "kind": "InlineFragment", "typeCondition": { "kind": "NamedType", "name": { "kind": "Name", "value": "ContractCall" } }, "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "Field", "name": { "kind": "Name", "value": "deploy" }, "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "Field", "name": { "kind": "Name", "value": "transaction" }, "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "Field", "name": { "kind": "Name", "value": "contractCalls" }, "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "Field", "name": { "kind": "Name", "value": "address" } }, { "kind": "Field", "name": { "kind": "Name", "value": "state" } }] } }] } }] } }] } }] } }] } }] };
const LatestContractTxBlockHeightQueryDocument = { "kind": "Document", "definitions": [{ "kind": "OperationDefinition", "operation": "query", "name": { "kind": "Name", "value": "LATEST_CONTRACT_TX_BLOCK_HEIGHT_QUERY" }, "variableDefinitions": [{ "kind": "VariableDefinition", "variable": { "kind": "Variable", "name": { "kind": "Name", "value": "address" } }, "type": { "kind": "NonNullType", "type": { "kind": "NamedType", "name": { "kind": "Name", "value": "String" } } } }], "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "Field", "name": { "kind": "Name", "value": "contract" }, "arguments": [{ "kind": "Argument", "name": { "kind": "Name", "value": "address" }, "value": { "kind": "Variable", "name": { "kind": "Name", "value": "address" } } }], "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "Field", "name": { "kind": "Name", "value": "transaction" }, "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "Field", "name": { "kind": "Name", "value": "block" }, "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "Field", "name": { "kind": "Name", "value": "height" } }] } }] } }] } }] } }] };
const TxsFromBlockSubDocument = { "kind": "Document", "definitions": [{ "kind": "OperationDefinition", "operation": "subscription", "name": { "kind": "Name", "value": "TXS_FROM_BLOCK_SUB" }, "variableDefinitions": [{ "kind": "VariableDefinition", "variable": { "kind": "Variable", "name": { "kind": "Name", "value": "offset" } }, "type": { "kind": "NamedType", "name": { "kind": "Name", "value": "BlockOffsetInput" } } }], "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "Field", "name": { "kind": "Name", "value": "blocks" }, "arguments": [{ "kind": "Argument", "name": { "kind": "Name", "value": "offset" }, "value": { "kind": "Variable", "name": { "kind": "Name", "value": "offset" } } }], "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "Field", "name": { "kind": "Name", "value": "hash" } }, { "kind": "Field", "name": { "kind": "Name", "value": "height" } }, { "kind": "Field", "name": { "kind": "Name", "value": "transactions" }, "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "Field", "name": { "kind": "Name", "value": "hash" } }, { "kind": "Field", "name": { "kind": "Name", "value": "identifiers" } }, { "kind": "Field", "name": { "kind": "Name", "value": "contractCalls" }, "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "Field", "name": { "kind": "Name", "value": "state" } }, { "kind": "Field", "name": { "kind": "Name", "value": "address" } }] } }] } }] } }] } }] };
const ContractStateQueryDocument = { "kind": "Document", "definitions": [{ "kind": "OperationDefinition", "operation": "query", "name": { "kind": "Name", "value": "CONTRACT_STATE_QUERY" }, "variableDefinitions": [{ "kind": "VariableDefinition", "variable": { "kind": "Variable", "name": { "kind": "Name", "value": "address" } }, "type": { "kind": "NonNullType", "type": { "kind": "NamedType", "name": { "kind": "Name", "value": "String" } } } }, { "kind": "VariableDefinition", "variable": { "kind": "Variable", "name": { "kind": "Name", "value": "offset" } }, "type": { "kind": "NamedType", "name": { "kind": "Name", "value": "BlockOffsetInput" } } }], "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "Field", "name": { "kind": "Name", "value": "contract" }, "arguments": [{ "kind": "Argument", "name": { "kind": "Name", "value": "address" }, "value": { "kind": "Variable", "name": { "kind": "Name", "value": "address" } } }, { "kind": "Argument", "name": { "kind": "Name", "value": "offset" }, "value": { "kind": "Variable", "name": { "kind": "Name", "value": "offset" } } }], "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "Field", "name": { "kind": "Name", "value": "state" } }] } }] } }] };
const ContractStateSubDocument = { "kind": "Document", "definitions": [{ "kind": "OperationDefinition", "operation": "subscription", "name": { "kind": "Name", "value": "CONTRACT_STATE_SUB" }, "variableDefinitions": [{ "kind": "VariableDefinition", "variable": { "kind": "Variable", "name": { "kind": "Name", "value": "address" } }, "type": { "kind": "NonNullType", "type": { "kind": "NamedType", "name": { "kind": "Name", "value": "String" } } } }, { "kind": "VariableDefinition", "variable": { "kind": "Variable", "name": { "kind": "Name", "value": "offset" } }, "type": { "kind": "NamedType", "name": { "kind": "Name", "value": "BlockOffsetInput" } } }], "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "Field", "name": { "kind": "Name", "value": "contract" }, "arguments": [{ "kind": "Argument", "name": { "kind": "Name", "value": "address" }, "value": { "kind": "Variable", "name": { "kind": "Name", "value": "address" } } }, { "kind": "Argument", "name": { "kind": "Name", "value": "offset" }, "value": { "kind": "Variable", "name": { "kind": "Name", "value": "offset" } } }], "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "Field", "name": { "kind": "Name", "value": "state" } }] } }] } }] };
const BothStateQueryDocument = { "kind": "Document", "definitions": [{ "kind": "OperationDefinition", "operation": "query", "name": { "kind": "Name", "value": "BOTH_STATE_QUERY" }, "variableDefinitions": [{ "kind": "VariableDefinition", "variable": { "kind": "Variable", "name": { "kind": "Name", "value": "address" } }, "type": { "kind": "NonNullType", "type": { "kind": "NamedType", "name": { "kind": "Name", "value": "String" } } } }, { "kind": "VariableDefinition", "variable": { "kind": "Variable", "name": { "kind": "Name", "value": "offset" } }, "type": { "kind": "NamedType", "name": { "kind": "Name", "value": "BlockOffsetInput" } } }], "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "Field", "name": { "kind": "Name", "value": "contract" }, "arguments": [{ "kind": "Argument", "name": { "kind": "Name", "value": "address" }, "value": { "kind": "Variable", "name": { "kind": "Name", "value": "address" } } }, { "kind": "Argument", "name": { "kind": "Name", "value": "offset" }, "value": { "kind": "Variable", "name": { "kind": "Name", "value": "offset" } } }], "selectionSet": { "kind": "SelectionSet", "selections": [{ "kind": "Field", "name": { "kind": "Name", "value": "state" } }, { "kind": "Field", "name": { "kind": "Name", "value": "zswapChainState" } }] } }] } }] };

/* eslint-disable */
/**
 * Map of all GraphQL operations in the project.
 *
 * This map has several performance disadvantages:
 * 1. It is not tree-shakeable, so it will include all operations in the project.
 * 2. It is not minifiable, so the string of a GraphQL query will be multiple times inside the bundle.
 * 3. It does not support dead code elimination, so it will add unused operations.
 *
 * Therefore it is highly recommended to use the babel or swc plugin for production.
 */
const documents = {
    "\n  query BLOCK_HASH_QUERY($offset: BlockOffsetInput) {\n    block(offset: $offset) {\n      height\n      hash\n    }\n  }": BlockHashQueryDocument,
    "\n  query TX_ID_QUERY($identifier: String!) {\n    transactions(identifier: $identifier) {\n      raw\n      applyStage\n      hash\n      block {\n        height\n        hash\n      }\n    }\n  }": TxIdQueryDocument,
    "\n  query DEPLOY_TX_QUERY($address: String!) {\n    contract(address: $address) {\n      ... on ContractDeploy {\n        transaction {\n\t        raw\n          applyStage\n          hash\n          identifiers\n          contractCalls {\n            address\n          }\n          block {\n            height\n            hash\n          }\n        }\n      }\n      ... on ContractCall {\n        deploy {\n          transaction {\n\t          raw\n            applyStage\n            hash\n            identifiers\n            contractCalls {\n              address\n            }\n            block {\n              height\n              hash\n            }\n          }\n        }\n      }\n    }\n  }": DeployTxQueryDocument,
    "\n  query DEPLOY_CONTRACT_STATE_TX_QUERY($address: String!) {\n    contract(address: $address) {\n      ... on ContractDeploy {\n        state\n      }\n      ... on ContractCall {\n        deploy {\n          transaction {\n            contractCalls {\n              address\n              state\n            }\n          }\n        }\n      }\n    }\n  }": DeployContractStateTxQueryDocument,
    "\n  query LATEST_CONTRACT_TX_BLOCK_HEIGHT_QUERY($address: String!) {\n    contract(address: $address) {\n      transaction {\n        block {\n          height\n        }\n      }\n    }\n  }": LatestContractTxBlockHeightQueryDocument,
    "\n  subscription TXS_FROM_BLOCK_SUB($offset: BlockOffsetInput) {\n    blocks(offset: $offset) {\n      hash,\n      height,\n      transactions {\n        hash\n        identifiers\n        contractCalls {\n          state\n          address\n        }\n      }\n    }\n  }": TxsFromBlockSubDocument,
    "\n  query CONTRACT_STATE_QUERY($address: String!, $offset: BlockOffsetInput) {\n    contract(address: $address, offset: $offset) {\n      state\n    }\n  }": ContractStateQueryDocument,
    "\n  subscription CONTRACT_STATE_SUB($address: String!, $offset: BlockOffsetInput) {\n    contract(address: $address, offset: $offset) {\n      state\n    }\n  }": ContractStateSubDocument,
    "\n  query BOTH_STATE_QUERY($address: String!, $offset: BlockOffsetInput) {\n    contract(address: $address, offset: $offset) {\n      state\n      zswapChainState\n    }\n  }": BothStateQueryDocument,
};
function gql(source) {
    return documents[source] ?? {};
}

const BLOCK_QUERY = gql(`
  query BLOCK_HASH_QUERY($offset: BlockOffsetInput) {
    block(offset: $offset) {
      height
      hash
    }
  }`);
const TX_ID_QUERY = gql(`
  query TX_ID_QUERY($identifier: String!) {
    transactions(identifier: $identifier) {
      raw
      applyStage
      hash
      block {
        height
        hash
      }
    }
  }`);
const DEPLOY_TX_QUERY = gql(`
  query DEPLOY_TX_QUERY($address: String!) {
    contract(address: $address) {
      ... on ContractDeploy {
        transaction {
	        raw
          applyStage
          hash
          identifiers
          contractCalls {
            address
          }
          block {
            height
            hash
          }
        }
      }
      ... on ContractCall {
        deploy {
          transaction {
	          raw
            applyStage
            hash
            identifiers
            contractCalls {
              address
            }
            block {
              height
              hash
            }
          }
        }
      }
    }
  }`);
const DEPLOY_CONTRACT_STATE_TX_QUERY = gql(`
  query DEPLOY_CONTRACT_STATE_TX_QUERY($address: String!) {
    contract(address: $address) {
      ... on ContractDeploy {
        state
      }
      ... on ContractCall {
        deploy {
          transaction {
            contractCalls {
              address
              state
            }
          }
        }
      }
    }
  }`);
const LATEST_CONTRACT_TX_BLOCK_HEIGHT_QUERY = gql(`
  query LATEST_CONTRACT_TX_BLOCK_HEIGHT_QUERY($address: String!) {
    contract(address: $address) {
      transaction {
        block {
          height
        }
      }
    }
  }`);
const TXS_FROM_BLOCK_SUB = gql(`
  subscription TXS_FROM_BLOCK_SUB($offset: BlockOffsetInput) {
    blocks(offset: $offset) {
      hash,
      height,
      transactions {
        hash
        identifiers
        contractCalls {
          state
          address
        }
      }
    }
  }`);
const CONTRACT_STATE_QUERY = gql(`
  query CONTRACT_STATE_QUERY($address: String!, $offset: BlockOffsetInput) {
    contract(address: $address, offset: $offset) {
      state
    }
  }`);
const CONTRACT_STATE_SUB = gql(`
  subscription CONTRACT_STATE_SUB($address: String!, $offset: BlockOffsetInput) {
    contract(address: $address, offset: $offset) {
      state
    }
  }`);
const CONTRACT_AND_ZSWAP_STATE_QUERY = gql(`
  query BOTH_STATE_QUERY($address: String!, $offset: BlockOffsetInput) {
    contract(address: $address, offset: $offset) {
      state
      zswapChainState
    }
  }`);

const maybeThrowGraphQLErrors = (result) => {
    if (result.errors && result.errors.length > 0) {
        throw new Error(result.errors.reduce((acc, graphQLError) => `${graphQLError.message}:\n\t${acc}`, ''));
    }
    return result;
};
const maybeThrowApolloError = (result) => {
    if (result.error) {
        throw new Error(result.error.message);
    }
    return result;
};
const maybeThrowErrors = (queryResult) => {
    maybeThrowApolloError(queryResult);
    return maybeThrowGraphQLErrors(queryResult);
};
const toByteArray = (s) => buffer.Buffer.from(s, 'hex');
const deserializeContractState = (s) => compactRuntime.ContractState.deserialize(toByteArray(s), midnightJsNetworkId.getRuntimeNetworkId());
const deserializeZswapState = (s) => ledger.ZswapChainState.deserialize(toByteArray(s), midnightJsNetworkId.getLedgerNetworkId());
const deserializeTransaction = (s) => ledger.Transaction.deserialize(toByteArray(s), midnightJsNetworkId.getLedgerNetworkId());
/**
 * This is a dirty hack. Prepends a network ID to the given contract address and
 * returns the result. As of ledger 3.0.0, the running node and indexer store
 * contract addresses with the network ID (in hex format) prepended to the address
 * while the ledger WASM API does not. So, before we query for a contract address,
 * we need to manually prepend the network ID to the contract address.
 *
 * @param contractAddress The contract address to which to prepend the network ID.
 */
const prependNetworkIdHex = (contractAddress) => `${midnightJsNetworkId.networkIdToHex(midnightJsNetworkId.getNetworkId())}${contractAddress}`;
const zenToRx = (zenObservable) => new Rx__namespace.Observable((subscriber) => zenObservable.subscribe(subscriber));
/**
 * The default time (in milliseconds) to wait between queries when polling.
 */
const DEFAULT_POLL_INTERVAL = 1000;
// Assumes that the block exists.
const blockOffsetToBlock$ = (apolloClient) => (offset) => zenToRx(apolloClient
    .subscribe({
    query: TXS_FROM_BLOCK_SUB,
    variables: {
        offset
    },
    fetchPolicy: 'no-cache'
})
    .map(maybeThrowGraphQLErrors)
    .map((fetchResult) => fetchResult.data.blocks));
const transactionIdToTransaction$ = (apolloClient) => (identifier) => zenToRx(apolloClient
    .watchQuery({
    query: TX_ID_QUERY,
    variables: {
        identifier
    },
    pollInterval: DEFAULT_POLL_INTERVAL,
    fetchPolicy: 'no-cache',
    initialFetchPolicy: 'no-cache',
    nextFetchPolicy: 'no-cache'
})
    .map(maybeThrowErrors)
    .filter((maybeQueryResult) => maybeQueryResult.data.transactions.length !== 0)
    .map((maybeQueryResult) => ({
    height: maybeQueryResult.data.transactions[0].block.height,
    hash: null
}))).pipe(Rx__namespace.concatMap(blockOffsetToBlock$(apolloClient)), Rx__namespace.concatMap(({ transactions }) => Rx__namespace.from(transactions)));
const transactionToContractState$ = (transactionId) => ({ identifiers, contractCalls }) => Rx__namespace.zip(identifiers, contractCalls).pipe(Rx__namespace.skipWhile((pair) => pair[0] !== transactionId), Rx__namespace.map((pair) => deserializeContractState(pair[1].state)));
const toTxStatus = (applyStage) => {
    if (applyStage === midnightJsTypes.FailEntirely ||
        applyStage === midnightJsTypes.FailFallible ||
        applyStage === midnightJsTypes.SucceedEntirely) {
        return applyStage;
    }
    throw new Error(`Unexpected 'applyStage' value ${applyStage}`);
};
const blockToContractState$ = (contractAddress) => (block) => Rx__namespace.from(block.transactions).pipe(Rx__namespace.concatMap(({ contractCalls }) => Rx__namespace.from(contractCalls)), Rx__namespace.filter((call) => call.address === contractAddress), Rx__namespace.map((call) => deserializeContractState(call.state)));
const contractAddressToLatestBlockOffset$ = (apolloClient) => (contractAddress) => zenToRx(apolloClient
    .watchQuery({
    query: LATEST_CONTRACT_TX_BLOCK_HEIGHT_QUERY,
    variables: {
        address: contractAddress
    },
    pollInterval: DEFAULT_POLL_INTERVAL,
    fetchPolicy: 'no-cache',
    initialFetchPolicy: 'no-cache',
    nextFetchPolicy: 'no-cache'
})
    .map(maybeThrowErrors)
    .filter((maybeQueryResult) => maybeQueryResult.data.contract !== null)
    .map((queryResult) => queryResult.data.contract.transaction.block.height)).pipe(Rx__namespace.take(1), Rx__namespace.map((height) => ({ height, hash: null })));
// Assumes block already exists
const blockOffsetToContractState$ = (apolloClient) => (contractAddress) => (offset) => zenToRx(apolloClient
    .subscribe({
    query: CONTRACT_STATE_SUB,
    variables: {
        address: contractAddress,
        offset
    },
    fetchPolicy: 'no-cache'
})
    .map(maybeThrowGraphQLErrors)
    .map((queryResult) => queryResult.data.contract.state)
    .map(deserializeContractState));
const waitForContractToAppear = (apolloClient) => (contractAddress) => (offset) => zenToRx(apolloClient
    .watchQuery({
    query: CONTRACT_STATE_QUERY,
    variables: {
        address: contractAddress,
        offset
    },
    pollInterval: DEFAULT_POLL_INTERVAL,
    fetchPolicy: 'no-cache',
    initialFetchPolicy: 'no-cache',
    nextFetchPolicy: 'no-cache'
})
    .map(maybeThrowErrors)
    .filter((maybeQueryResult) => maybeQueryResult.data.contract !== null)
    .map((queryResult) => queryResult.data.contract.state)).pipe(Rx__namespace.take(1));
const waitForBlockToAppear = (apolloClient) => (offset) => zenToRx(apolloClient
    .watchQuery({
    query: BLOCK_QUERY,
    variables: {
        offset
    },
    pollInterval: DEFAULT_POLL_INTERVAL,
    fetchPolicy: 'no-cache',
    initialFetchPolicy: 'no-cache',
    nextFetchPolicy: 'no-cache'
})
    .map(maybeThrowErrors)
    .filter((fetchResult) => fetchResult.data.block !== null)).pipe(Rx__namespace.take(1));
const indexerPublicDataProviderInternal = (queryURL, subscriptionURL, webSocketImpl = ws__namespace.WebSocket) => {
    const queryURLObj = new URL(queryURL);
    if (queryURLObj.protocol !== 'http:' && queryURLObj.protocol !== 'https:') {
        throw new Error(`Invalid protocol: ${queryURLObj.protocol}`);
    }
    const subscriptionURLObj = new URL(subscriptionURL);
    if (subscriptionURLObj.protocol !== 'ws:' && subscriptionURLObj.protocol !== 'wss:') {
        throw new Error(`Invalid protocol: ${subscriptionURLObj.protocol}`);
    }
    const apolloClient = new index_js.ApolloClient({
        link: index_js$1.split(({ query }) => {
            const definition = index_js$2.getMainDefinition(query);
            return definition.kind === 'OperationDefinition' && definition.operation === 'subscription';
        }, new index_js$3.GraphQLWsLink(graphqlWs.createClient({ url: subscriptionURL, webSocketImpl })), index_js$4.createHttpLink({ fetch, uri: queryURL })),
        cache: new index_js.InMemoryCache()
    });
    return {
        async queryContractState(address, config) {
            let offset;
            if (config) {
                offset =
                    config.type === 'blockHeight'
                        ? { height: config.blockHeight, hash: null }
                        : { height: null, hash: config.blockHash };
            }
            else {
                offset = null;
            }
            const maybeContractState = await apolloClient
                .query({
                query: CONTRACT_STATE_QUERY,
                variables: {
                    address,
                    offset
                },
                fetchPolicy: 'no-cache'
            })
                .then(maybeThrowErrors)
                .then((queryResult) => queryResult.data?.contract?.state ?? null);
            return maybeContractState ? deserializeContractState(maybeContractState) : null;
        },
        async queryZSwapAndContractState(address, config) {
            let offset;
            if (config) {
                offset =
                    config.type === 'blockHeight'
                        ? { height: config.blockHeight, hash: null }
                        : { height: null, hash: config.blockHash };
            }
            else {
                offset = null;
            }
            const maybeContractStates = await apolloClient
                .query({
                query: CONTRACT_AND_ZSWAP_STATE_QUERY,
                variables: {
                    address,
                    offset
                },
                fetchPolicy: 'no-cache'
            })
                .then(maybeThrowErrors)
                .then((queryResult) => queryResult.data.contract);
            return maybeContractStates
                ? [
                    deserializeZswapState(maybeContractStates.zswapChainState),
                    deserializeContractState(maybeContractStates.state)
                ]
                : null;
        },
        async queryDeployContractState(contractAddress) {
            return apolloClient
                .query({
                query: DEPLOY_CONTRACT_STATE_TX_QUERY,
                variables: {
                    address: contractAddress
                },
                fetchPolicy: 'no-cache'
            })
                .then((queryResult) => {
                if (queryResult.data.contract) {
                    return 'deploy' in queryResult.data.contract
                        ? queryResult.data.contract.deploy.transaction.contractCalls.find(({ address }) => address === contractAddress).state
                        : queryResult.data.contract.state;
                }
                return null;
            })
                .then((maybeContractState) => maybeContractState ? deserializeContractState(maybeContractState) : null);
        },
        async watchForContractState(contractAddress) {
            return Rx__namespace.firstValueFrom(waitForContractToAppear(apolloClient)(contractAddress)(null).pipe(Rx__namespace.map(deserializeContractState)));
        },
        async watchForDeployTxData(contractAddress) {
            return Rx__namespace.firstValueFrom(zenToRx(apolloClient
                .watchQuery({
                query: DEPLOY_TX_QUERY,
                variables: {
                    address: contractAddress
                },
                pollInterval: DEFAULT_POLL_INTERVAL,
                fetchPolicy: 'no-cache',
                initialFetchPolicy: 'no-cache',
                nextFetchPolicy: 'no-cache'
            })
                .filter((maybeQueryResult) => maybeQueryResult.data.contract !== null)
                .map(maybeThrowErrors)
                .map((queryResult) => 'deploy' in queryResult.data.contract
                ? queryResult.data.contract.deploy.transaction
                : queryResult.data.contract.transaction)
                .map((transaction) => ({
                tx: deserializeTransaction(transaction.raw),
                status: toTxStatus(transaction.applyStage),
                txId: transaction.identifiers[transaction.contractCalls.findIndex(({ address }) => address === contractAddress)],
                txHash: transaction.hash,
                blockHeight: transaction.block.height,
                blockHash: transaction.block.hash
            }))));
        },
        async watchForTxData(txId) {
            return Rx__namespace.firstValueFrom(zenToRx(apolloClient
                .watchQuery({
                query: TX_ID_QUERY,
                variables: { identifier: txId },
                pollInterval: DEFAULT_POLL_INTERVAL,
                fetchPolicy: 'no-cache',
                initialFetchPolicy: 'no-cache',
                nextFetchPolicy: 'no-cache'
            })
                .map(maybeThrowErrors)
                .filter((maybeQueryResult) => maybeQueryResult.data.transactions.length !== 0)
                .map((queryResult) => queryResult.data.transactions[0])
                .map((transaction) => ({
                tx: deserializeTransaction(transaction.raw),
                status: toTxStatus(transaction.applyStage),
                txId,
                txHash: transaction.hash,
                blockHeight: transaction.block.height,
                blockHash: transaction.block.hash
            }))));
        },
        contractStateObservable(contractAddress, config = { type: 'latest' }) {
            if (config.type === 'txId') {
                const contractStates = transactionIdToTransaction$(apolloClient)(config.txId).pipe(Rx__namespace.concatMap(transactionToContractState$(config.txId)));
                return (config.inclusive ?? true) ? contractStates : contractStates.pipe(Rx__namespace.skip(1));
            }
            if (config.type === 'latest') {
                return contractAddressToLatestBlockOffset$(apolloClient)(contractAddress).pipe(Rx__namespace.concatMap(blockOffsetToBlock$(apolloClient)), Rx__namespace.concatMap(blockToContractState$(contractAddress)));
            }
            if (config.type === 'all') {
                return waitForContractToAppear(apolloClient)(contractAddress)(null).pipe(Rx__namespace.concatMap(() => blockOffsetToContractState$(apolloClient)(contractAddress)(null)));
            }
            const offset = config.type === 'blockHash'
                ? { height: null, hash: config.blockHash }
                : { height: config.blockHeight, hash: null };
            const blocks = waitForBlockToAppear(apolloClient)(offset).pipe(Rx__namespace.concatMap(() => blockOffsetToBlock$(apolloClient)(offset)));
            const maybeShortenedBlocks = config.type === 'blockHeight' || config.type === 'blockHash'
                ? Rx__namespace.iif(() => config.inclusive ?? true, blocks, blocks.pipe(Rx__namespace.skip(1)))
                : blocks;
            return maybeShortenedBlocks.pipe(Rx__namespace.concatMap(blockToContractState$(contractAddress)));
        }
    };
};
/**
 * Constructs a {@link PublicDataProvider} based on an {@link ApolloClient}.
 *
 * @param queryURL The URL of a GraphQL server query endpoint.
 * @param subscriptionURL The URL of a GraphQL server subscription (websocket) endpoint.
 * @param webSocketImpl An optional websocket implementation for the Apollo client to use.
 *
 * TODO: Re-examine caching when 'ContractCall' and 'ContractDeploy' have transaction identifiers included.
 */
const indexerPublicDataProvider = (queryURL, subscriptionURL, webSocketImpl = ws__namespace.WebSocket) => {
    /**
     * This current object is a wrapper around the real implementation of the indexer client constructed
     * below. This wrapper just asserts that the input contract addresses are valid, and prepends the hex
     * representation of the network ID to all input contract addresses to work around a discrepancy
     * as of ledger 3.0.0 between the contract address representation on the indexer (with network ID)
     * and the address representation in the ledger WASM API (without network ID).
     */
    const publicDataProvider = indexerPublicDataProviderInternal(queryURL, subscriptionURL, webSocketImpl);
    return {
        contractStateObservable(contractAddress, config) {
            midnightJsUtils.assertIsContractAddress(contractAddress);
            return publicDataProvider.contractStateObservable(prependNetworkIdHex(contractAddress), config);
        },
        queryContractState(contractAddress, config) {
            midnightJsUtils.assertIsContractAddress(contractAddress);
            return publicDataProvider.queryContractState(prependNetworkIdHex(contractAddress), config);
        },
        queryDeployContractState(contractAddress) {
            midnightJsUtils.assertIsContractAddress(contractAddress);
            return publicDataProvider.queryDeployContractState(prependNetworkIdHex(contractAddress));
        },
        queryZSwapAndContractState(contractAddress, config) {
            midnightJsUtils.assertIsContractAddress(contractAddress);
            return publicDataProvider.queryZSwapAndContractState(prependNetworkIdHex(contractAddress), config);
        },
        watchForContractState(contractAddress) {
            midnightJsUtils.assertIsContractAddress(contractAddress);
            return publicDataProvider.watchForContractState(prependNetworkIdHex(contractAddress));
        },
        watchForDeployTxData(contractAddress) {
            midnightJsUtils.assertIsContractAddress(contractAddress);
            return publicDataProvider.watchForDeployTxData(prependNetworkIdHex(contractAddress));
        },
        watchForTxData(txId) {
            return publicDataProvider.watchForTxData(txId);
        }
    };
};

exports.indexerPublicDataProvider = indexerPublicDataProvider;
//# sourceMappingURL=index.cjs.map
