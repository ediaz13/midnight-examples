import { JestEnvironment } from "@jest/environment";
import { Link, LinkType, MetadataMessage } from "allure-js-commons";
export interface AllureEnvironment extends JestEnvironment {
    transformLinks(links: Link[]): Link[];
    handleAllureMetadata(payload: {
        currentTestName: string;
        metadata: MetadataMessage;
    }): void;
}
export interface LinkMatcher {
    type: LinkType | string;
    urlTemplate: string;
}
declare const createJestEnvironment: <T extends typeof JestEnvironment>(Base: T) => T;
export default createJestEnvironment;
